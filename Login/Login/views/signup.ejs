<!DOCTYPE html>
<html lang="en">
    <style>
.makebox
       {
           width: 25%;
           border-radius: 10px;
           font-size: 18px;
           background-color: rgb(91, 91, 151);
           margin: auto;
           border: solid;
       }
       .bgimg{
           background-image:url(Signup.jpg);
           background-repeat: no-repeat;
           background-size: 2000px 768px;
       }
       #ctr
       {
           align-items: center;
           align-self: center;
       }
       .button
       {
        font-size: 14px;
           padding: 10px;
           margin: 5px;
           border-radius: 10px;
       }
       .button:hover{
           background-color: rgb(90, 90, 138);
           transition: all 0.2s ease-in-out;
           color: black;
           box-shadow: 0 12px 16px 0 rgba(165, 148, 148, 0.315), 0 17px 50px 0 rgba(165, 148, 148, 0.315);
           cursor: pointer;
       }
    </style>
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyBuddy-Register</title>
</head>
<script>
function verifyPassword() {  
    var pw = document.getElementById("pw").value;
    var p = document.getElementById("pw1").value;     
   //minimum password length validation  
    if(pw.length < 8) {  
       //document.getElementById("message").innerHTML = "**Password length must be atleast 8 characters";
       alert("Password length must be atleast 8 characters");
       event.preventDefault();  
       return false;  
    }  
    
  //maximum length of password validation  
    if(pw.length > 15) {  
       //document.getElementById("message").innerHTML = "**Password length must not exceed 15 characters";  
       alert("Password length must not exceed 15 characters");
       event.preventDefault();
       return false;  
    }
    if(p!=pw)  
  {   
    alert("Passwords did not match");
    event.preventDefault();
    return false;  
  } 
  else {  
    alert("Password created successfully");
    return true;  
  }   
}
          
  </script>
<body class="bgimg">
    <h1 align="center">Welcome to the MyBuddy Sign Up page<hr></h1>
    <form onsubmit="verifyPassword()" action="#" method="post">
<br><br><br>
<div class="makebox" align="center">
<pre><label for="Register">Sign Up</label></p></pre><pre>
First Name*  <input type="text" name="fn" placeholder="Enter your first name" required><br><br>Middle Name* <input type="text" name="mn" placeholder="Enter your Middle name" required><br><br>Last Name*   <input type="text" name="ln" placeholder="Enter your Last name" required></pre>
<pre>DOB*      <input type="date" name="dob" required></pre>
<pre>Phone No.*   <input type="number" name="phn" placeholder="Enter your phone number" required></pre>
<pre>Email ID     <input type="email" name="em" placeholder="Enter your email id"></pre>
<pre>User Name*   <input type="text" name="un" placeholder="Set your username" required></pre>
<pre>Password*    <input type="password" name="pw" id="pw" placeholder="Set a strong password" required>

             <input type="password" name="pw1" id="pw1" placeholder="Confirm Password" required></pre>
             <p>Note:Fields marked with * are required</p>
<button type="submit" class="button">Submit</button><br>
</div>
    </form>
</body>
</html>